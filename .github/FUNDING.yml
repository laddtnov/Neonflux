# Renders the "Sponsor" button at the top of the repository page.
# The link that reaches actual users is fundingUrl in manifest.json — Obsidian
# shows it inside the theme's own settings entry, and most people who install
# a theme never open its repository.
custom: ["https://buymeacoffee.com/laddtnov"]
